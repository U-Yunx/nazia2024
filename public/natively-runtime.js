// Platform preview runtime stub.
// Injected by the host as `<script type="module" src="/natively-runtime.js">`
// so the asset must exist for the Vite build/dev server to resolve it.
// The real runtime is provided by the hosting platform at runtime.